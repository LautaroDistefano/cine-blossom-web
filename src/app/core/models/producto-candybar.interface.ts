export interface ProductoCandyBar{
    // id, nombre, descripcion, precio, categoria, imagen, disponible.
    id: string,
    nombre: string,
    descripcion: string,
    precio: number,
    categoria: string,
    imagen: string,
    disponible: boolean 
}